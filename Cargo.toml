[package]
name = "hyprrun"
version = "0.1.1"
edition = "2021"

[profile.release]
strip = true

[dependencies]
argh = "0.1"
anyhow = "1.0"
miniserde = "0.1"
regex = "1.12.2"
log = { version = "0.4", features = ["std"] }
chrono = "0.4"
